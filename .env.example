# ============================================================================
# Agent Toolkit - Environment Configuration & API Keys
# ============================================================================
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed
#
# This file supports both standard format (KEY=value) and natural language:
# - Standard: OPENAI_API_KEY=sk-...
# - Natural: OpenAI API key: sk-...
# - Natural: Airtable personal access token: pat...
#
# Agents will search for matching keys using fuzzy matching, so you can use
# human-readable names like "Discord bot key" or "GitHub token"
# ============================================================================

# ----------------------------------------------------------------------------
# LLM Provider Configuration
# ----------------------------------------------------------------------------
# Choose your LLM provider: "lm_studio" (default) or "litellm"
LLM_PROVIDER=lm_studio

# LM Studio (local inference)
LM_STUDIO_HOST=localhost
LM_STUDIO_PORT=1234
LM_STUDIO_MODEL=local-model

# LiteLLM (cloud providers) - uncomment and configure as needed
# LITELLM_MODEL=gpt-4
# OpenAI API key:
# Anthropic API key:
# Azure OpenAI API key:

# ----------------------------------------------------------------------------
# Common API Keys & Tokens
# ----------------------------------------------------------------------------
# The agent will recognize these with natural language matching
# Examples: "airtable", "discord", "github", "stripe", etc.

# Airtable
# Airtable personal access token:

# Discord
# Discord bot token:
# Discord bot key:

# GitHub
# GitHub personal access token:
# GitHub API token:

# Stripe
# Stripe API key:
# Stripe secret key:

# Notion
# Notion API key:
# Notion integration token:

# Slack
# Slack bot token:
# Slack API token:

# Google
# Google API key:
# Google service account:

# Twitter/X
# Twitter API key:
# Twitter bearer token:

# OpenAI (if using via direct API, not LiteLLM)
# OpenAI API key:

# Anthropic (if using via direct API, not LiteLLM)
# Anthropic API key:

# ----------------------------------------------------------------------------
# Custom API Keys
# ----------------------------------------------------------------------------
# Add your own API keys below using natural language
# The agent will match based on keywords in the name
#
# Examples:
# My Custom Service API key: xxx
# Internal Tool authentication token: yyy
# Production database password: zzz

# ----------------------------------------------------------------------------
# Server Configuration (Optional)
# ----------------------------------------------------------------------------
# Uncomment to override defaults
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5000
# FLASK_DEBUG=True

# ----------------------------------------------------------------------------
# Agent Behavior Configuration (Optional)
# ----------------------------------------------------------------------------
# MAX_CONTENT_LENGTH=4000
# REQUEST_TIMEOUT=10
# SUMMARY_MAX_TOKENS=500
# SUMMARY_TEMPERATURE=0.7
